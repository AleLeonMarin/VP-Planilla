---
phase: 13-integracion-frontend-backend
plan: 09
type: gap-closure
gap_closure: true
wave: 1
depends_on: []
files_modified:
  - src/frontend/src/hooks/useEmployeeList.ts
  - src/frontend/src/app/pages/employee/list/page.tsx
  - src/frontend/src/components/EmployeeTable.tsx
autonomous: true
requirements: [UI-POLISH-01]
user_setup: []

must_haves:
  truths:
    - "Employee list page muestra skeleton loading mientras cargan datos"
    - "Hook useEmployeeList expone isLoading para fetch inicial"
  artifacts:
    - path: "src/frontend/src/hooks/useEmployeeList.ts"
      provides: "isLoading state para fetch inicial"
      contains: "isLoading"
    - path: "src/frontend/src/app/pages/employee/list/page.tsx"
      provides: "Skeleton loading para stats cards y employee table"
      contains: "animate-pulse"
---

<objective>
Add skeleton loading states to employee list page — hook lacks isLoading for initial fetch, page doesn't show skeletons.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add isLoading to useEmployeeList hook and skeleton to employee list page</name>
  <files>
    src/frontend/src/hooks/useEmployeeList.ts
    src/frontend/src/app/pages/employee/list/page.tsx
    src/frontend/src/components/EmployeeTable.tsx
  </files>
  <action>
    1. In useEmployeeList.ts:
       - Add `const [isLoading, setIsLoading] = useState(false)` at top
       - Set `setIsLoading(true)` before `apiGetEmployees()` call
       - Set `setIsLoading(false)` in finally block
       - Export `isLoading` in return object
       - Also add `error` state for fetch failures

    2. In employee/list/page.tsx:
       - Destructure `isLoading` and `error` from hook
       - Add error banner (same pattern as positions error)
       - Add skeleton loading for EmployeeStatsCards (4 skeleton cards with animate-pulse)
       - Add skeleton loading for EmployeeTable (5 skeleton rows with animate-pulse)
       - Only show skeletons during initial load (isLoading && employees.length === 0)

    3. In EmployeeTable.tsx (if needed):
       - Accept `isLoading` prop
       - Show skeleton table rows when loading
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    Visual check: Employee list shows skeleton cards and table rows during initial load
  </verify>
  <done>Employee list page shows proper skeleton loading for stats cards and table, with error banner support</done>
</task>

</tasks>
