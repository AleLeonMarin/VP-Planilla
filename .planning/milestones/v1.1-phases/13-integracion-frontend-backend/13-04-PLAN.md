---
phase: 13-integracion-frontend-backend
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/frontend/src/app/pages/positions/list/page.tsx
  - src/frontend/src/app/pages/bonuses/list/page.tsx
  - src/frontend/src/app/pages/employee/events/page.tsx
  - src/frontend/src/app/pages/payroll-types/list/page.tsx
autonomous: true
requirements: [INTEG-02]
gap_closure: true

must_haves:
  truths:
    - "User sees toast.success() notification on successful CRUD operations"
    - "User sees toast.error() notification on failed CRUD operations"
    - "No custom modal dialogs appear for success/error feedback on CRUD operations"
  artifacts:
    - path: "src/frontend/src/app/pages/positions/list/page.tsx"
      provides: "Positions page using toast notifications"
      min_lines: 130
    - path: "src/frontend/src/app/pages/bonuses/list/page.tsx"
      provides: "Bonuses page using toast notifications"
      min_lines: 130
    - path: "src/frontend/src/app/pages/employee/events/page.tsx"
      provides: "Labor events page using toast notifications"
      min_lines: 250
    - path: "src/frontend/src/app/pages/payroll-types/list/page.tsx"
      provides: "Payroll types page using toast notifications"
      min_lines: 290
  key_links:
    - from: "page files"
      to: "sonner toast"
      via: "import { toast } from 'sonner'"
      pattern: "toast\\.(success|error)"
---

<objective>
Replace all modal-based success/error notifications with sonner toast notifications across 4 page files.

Purpose: UAT found that CRUD operations show a custom modal dialog with "Entendido" button instead of toast notifications. The Toaster is already mounted in `src/frontend/src/layouts/main.tsx` — only the page code needs to call `toast.success()` / `toast.error()` instead of `modal.showSuccess()` / `modal.showError()`.

Output: 4 page files updated, no new files created, no new dependencies added.
</objective>

<execution_context>
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-integracion-frontend-backend/13-UAT.md
@.planning/phases/13-integracion-frontend-backend/13-02-SUMMARY.md
@src/frontend/src/app/pages/positions/list/page.tsx
@src/frontend/src/app/pages/bonuses/list/page.tsx
@src/frontend/src/app/pages/employee/events/page.tsx
@src/frontend/src/app/pages/payroll-types/list/page.tsx
@src/frontend/src/hooks/useModal.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace modal notifications with toast in positions and bonuses pages</name>
  <files>src/frontend/src/app/pages/positions/list/page.tsx, src/frontend/src/app/pages/bonuses/list/page.tsx</files>
  <action>
For BOTH files, make these changes:

1. **Add toast import**: Add `import { toast } from 'sonner';` at the top of the file (after existing imports).

2. **Remove useModal import and instance**: Remove `import { useModal } from '@/hooks/useModal';` and the `const modal = useModal();` line.

3. **Replace success calls**:
   - `modal.showSuccess('Actualizado', 'Posición actualizada correctamente')` → `toast.success('Posición actualizada correctamente')`
   - `modal.showSuccess('Creado', 'Posición creada correctamente')` → `toast.success('Posición creada correctamente')`
   - `modal.showSuccess('Eliminado', 'Posición eliminada correctamente')` → `toast.success('Posición eliminada correctamente')`
   - Same pattern for bonuses: 'Bonificación actualizada/creada/eliminada correctamente'

4. **Replace error calls**:
   - `modal.showError('Error', err instanceof Error ? err.message : 'Error al guardar')` → `toast.error(err instanceof Error ? err.message : 'Error al guardar')`
   - `modal.showError('Error', err instanceof Error ? err.message : 'Error al eliminar')` → `toast.error(err instanceof Error ? err.message : 'Error al eliminar')`

5. **Remove ModalComponent**: In positions page there is no `<modal.ModalComponent />` — verify none exists. In bonuses page same.

Gap reason: Pages use modal.showSuccess/showError which renders custom Modal dialog with "Entendido" button instead of sonner toasts.
  </action>
  <verify>
    <automated>npx tsc --noEmit in src/frontend/</automated>
  </verify>
  <done>Both files import toast from sonner, call toast.success/toast.error for all CRUD feedback, no useModal references remain for success/error notifications</done>
</task>

<task type="auto">
  <name>Task 2: Replace modal notifications with toast in employee events and payroll types pages</name>
  <files>src/frontend/src/app/pages/employee/events/page.tsx, src/frontend/src/app/pages/payroll-types/list/page.tsx</files>
  <action>
For BOTH files, make these changes:

**employee/events/page.tsx:**
1. Add `import { toast } from 'sonner';` at the top
2. Replace `const { showError, showSuccess } = useModal();` → remove the useModal import entirely, remove this line
3. Replace `showSuccess('Éxito', 'Evento actualizado correctamente')` → `toast.success('Evento actualizado correctamente')`
4. Replace `showSuccess('Éxito', 'Evento creado correctamente')` → `toast.success('Evento creado correctamente')`
5. Replace `showError('Error', 'No se pudo guardar el evento. Por favor intente nuevamente.')` → `toast.error('No se pudo guardar el evento. Por favor intente nuevamente.')`
6. Remove the `useModal` import line

**payroll-types/list/page.tsx:**
1. Add `import { toast } from 'sonner';` at the top
2. Remove `import { useModal } from '@/hooks/useModal';` and `const modal = useModal();`
3. Replace all `modal.showSuccess('...', '...')` → `toast.success('...')` (only the message, drop the title)
4. Replace all `modal.showError('Error', ...)` → `toast.error(...)` (only the message)
5. Remove `<modal.ModalComponent />` near the bottom of the file (line ~298) — this renders the modal that must go

Specific replacements for payroll-types:
- `modal.showSuccess('Actualizado', 'Tipo de planilla actualizado correctamente')` → `toast.success('Tipo de planilla actualizado correctamente')`
- `modal.showSuccess('Creado', 'Tipo de planilla creado correctamente')` → `toast.success('Tipo de planilla creado correctamente')`
- `modal.showSuccess('Eliminado', 'Tipo de planilla eliminado correctamente')` → `toast.success('Tipo de planilla eliminado correctamente')`
- `modal.showError('Error', err instanceof Error ? err.message : 'Error al guardar')` → `toast.error(err instanceof Error ? err.message : 'Error al guardar')`
- `modal.showError('Error', err instanceof Error ? err.message : 'Error al eliminar. Esta funcionalidad aún no está disponible.')` → `toast.error(err instanceof Error ? err.message : 'Error al eliminar. Esta funcionalidad aún no está disponible.')`

Gap reason: Same root cause as positions/bonuses — modal dialogs instead of toasts. Payroll-types page additionally has `<modal.ModalComponent />` that must be removed.
  </action>
  <verify>
    <automated>npx tsc --noEmit in src/frontend/</automated>
  </verify>
  <done>All 4 pages use toast.success/toast.error for CRUD feedback, no useModal references for success/error, no ModalComponent rendered</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes in src/frontend/
- No remaining `modal.showSuccess` or `modal.showError` calls in any of the 4 page files
- No remaining `useModal` imports in these files (unless used for other purposes like confirmations — check each file)
- `toast.success` and `toast.error` calls present for all CRUD success/error paths
</verification>

<success_criteria>
- All 4 page files replaced modal notifications with sonner toast notifications
- TypeScript compiles without errors
- User sees toast notifications instead of modal dialogs on CRUD operations
</success_criteria>

<output>
After completion, create `.planning/phases/13-integracion-frontend-backend/13-04-SUMMARY.md`
</output>
