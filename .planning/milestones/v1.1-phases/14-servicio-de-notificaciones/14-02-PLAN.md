---
phase: 14-servicio-de-notificaciones
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - src/frontend/src/types/notification.ts
  - src/frontend/src/services/notificationService.ts
  - src/frontend/src/hooks/useNotifications.ts
  - src/frontend/src/components/ui/NotificationPanel.tsx
  - src/frontend/src/components/ui/Header.tsx
  - src/frontend/src/app/pages/notifications/page.tsx
autonomous: true
requirements:
  - NOTIF-03
  - NOTIF-04
must_haves:
  truths:
    - "El Header muestra un badge con el conteo real de notificaciones no leídas del usuario"
    - "Al hacer clic en la campana del Header se abre un panel con notificaciones reales de la API"
    - "Al hacer clic en una notificación se marca como leída"
    - "Existe una página /pages/notifications que muestra todas las notificaciones con paginación"
    - "Las notificaciones se actualizan al recargar la página (polling cada 30s)"
  artifacts:
    - path: "src/frontend/src/types/notification.ts"
      provides: "TypeScript interface Notification y NotificationListResponse"
      exports: ["Notification", "NotificationListResponse"]
    - path: "src/frontend/src/services/notificationService.ts"
      provides: "Service methods usando http.ts para llamadas API"
      exports: ["NotificationService"]
    - path: "src/frontend/src/hooks/useNotifications.ts"
      provides: "Hook con data, isLoading, error, actions"
      exports: ["useNotifications"]
    - path: "src/frontend/src/components/ui/NotificationPanel.tsx"
      provides: "Panel de notificaciones con AnimatePresence + motion.div"
      exports: ["NotificationPanel"]
    - path: "src/frontend/src/components/ui/Header.tsx"
      provides: "Header modificado para usar useNotifications en vez de datos hardcodeados"
    - path: "src/frontend/src/app/pages/notifications/page.tsx"
      provides: "Página completa de notificaciones con paginación y marcar todas como leídas"
  key_links:
    - from: "src/frontend/src/services/notificationService.ts"
      to: "/api/notifications"
      via: "http.get / http.put"
      pattern: "http\\.(get|put|post).*notifications"
    - from: "src/frontend/src/hooks/useNotifications.ts"
      to: "src/frontend/src/services/notificationService.ts"
      via: "useCallback que llama al servicio"
      pattern: "NotificationService\\."
    - from: "src/frontend/src/components/ui/Header.tsx"
      to: "src/frontend/src/hooks/useNotifications.ts"
      via: "useNotifications hook"
      pattern: "useNotifications"
    - from: "src/frontend/src/components/ui/NotificationPanel.tsx"
      to: "src/frontend/src/hooks/useNotifications.ts"
      via: "props o hook directo"
      pattern: "useNotifications|markAsRead"
---

<objective>
Integrar el servicio de notificaciones del backend en el frontend: types, service, hook, panel de notificaciones en Header, y página dedicada de notificaciones.

Purpose: Reemplazar las notificaciones hardcodeadas del Header con datos reales de la API, y crear una página dedicada para ver el historial completo.
Output: Types, service, hook, NotificationPanel, Header actualizado, página /notifications.
</objective>

<execution_context>
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-servicio-de-notificaciones/14-01-SUMMARY.md
@.planning/phases/14-servicio-de-notificaciones/14-01-PLAN.md
@src/frontend/src/components/ui/Header.tsx
@src/frontend/src/services/http.ts
@src/frontend/src/hooks/useAuditLogs.ts
@src/frontend/src/services/auditLogsService.ts
@src/frontend/src/layouts/main.tsx
@src/frontend/src/components/ui/ConfirmDialog.tsx

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/frontend/src/services/http.ts:
```typescript
export const http = {
  get: (path: string) => Promise<any>,
  post: (path: string, body?: unknown) => Promise<any>,
  put: (path: string, body?: unknown) => Promise<any>,
  delete: (path: string, body?: unknown) => Promise<any>,
};
```

From src/frontend/src/hooks/useAuth.tsx:
```typescript
interface User {
  user_id: number;
  user_first_name: string;
  user_last_name: string;
  user_middle_name: string;
  user_email: string;
  user_username: string;
  user_role: string;
}
```

From src/frontend/src/layouts/main.tsx:
- Header receives `onMenuClick` prop
- sonner Toaster is already configured at root level
- AnimatePresence + motion.div pattern used for modals

Pattern from useAuditLogs.ts (hook pattern to follow):
```typescript
export const useAuditLogs = () => {
  const [data, setData] = useState<AuditLog[]>([]);
  const [isLoading, setIsLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);
  const fetchAuditLogs = useCallback(async (filters?: AuditLogFilters) => { ... };
  return { data, isLoading, error, fetchAuditLogs, refetch };
};
```

Backend API endpoints (from 14-01-PLAN.md):
- GET /api/notifications?page=1&limit=20 → { data: Notification[], total: number }
- GET /api/notifications/unread-count → { count: number }
- PUT /api/notifications/:id/read → { success: true, data: Notification }
- PUT /api/notifications/read-all → { success: true, count: number }
- DELETE /api/notifications/:id → 204
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create notification types, service, and hook</name>
  <files>src/frontend/src/types/notification.ts, src/frontend/src/services/notificationService.ts, src/frontend/src/hooks/useNotifications.ts</files>
  <action>Create three files following existing patterns:

1. src/frontend/src/types/notification.ts:
```typescript
export interface Notification {
  notifications_id: number;
  notifications_user_id: number;
  notifications_title: string;
  notifications_message: string;
  notifications_type: string;
  notifications_is_read: boolean;
  notifications_created_at: string;
  notifications_version: number;
}

export interface NotificationListResponse {
  data: Notification[];
  total: number;
}
```

2. src/frontend/src/services/notificationService.ts — Use http.ts (NOT raw fetch):
- `getNotifications(page?: number, limit?: number): Promise<NotificationListResponse>` — GET /notifications?page=&limit=
- `getUnreadCount(): Promise<number>` — GET /notifications/unread-count
- `markAsRead(id: number): Promise<Notification>` — PUT /notifications/:id/read
- `markAllAsRead(): Promise<number>` — PUT /notifications/read-all
- `deleteNotification(id: number): Promise<void>` — DELETE /notifications/:id

Follow the pattern from auditLogsService.ts but use http.ts instead of raw fetch.

3. src/frontend/src/hooks/useNotifications.ts — Hook following useAuditLogs.ts pattern:
- State: data (Notification[]), total, isLoading, error, unreadCount
- Actions (useCallback): fetchNotifications(page, limit), fetchUnreadCount(), markAsRead(id), markAllAsRead(), deleteNotification(id)
- Polling: useEffect with setInterval(30000) to refresh unread count when component mounted
- Return: { data, total, isLoading, error, unreadCount, fetchNotifications, fetchUnreadCount, markAsRead, markAllAsRead, deleteNotification }
- Use toast.success/toast.error from sonner for action feedback (per decision [13])</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Types, service y hook creados, tsc sin errores, hook usa http.ts no fetch directo</done>
</task>

<task type="auto">
  <name>Task 2: Create NotificationPanel component and update Header to use real API</name>
  <files>src/frontend/src/components/ui/NotificationPanel.tsx, src/frontend/src/components/ui/Header.tsx</files>
  <action>Create NotificationPanel and update Header:

1. src/frontend/src/components/ui/NotificationPanel.tsx — New component:
- Props: { open: boolean, onClose: () => void, notifications: Notification[], unreadCount: number, onMarkRead: (id: number) => void, onMarkAllRead: () => void, isLoading: boolean }
- Typed as React.FC<PropsInterface>
- Uses AnimatePresence + motion.div with backdrop/modal animation pattern (per CLAUDE.md modal conventions)
- Shows loading skeleton when isLoading=true
- Shows empty state when no notifications
- Lists notifications with unread indicator (blue dot), title, message, and relative time
- Clicking a notification calls onMarkRead(id)
- "Marcar todas como leídas" button calls onMarkAllRead
- Dark mode consistent with existing UI (zinc-950, zinc-900, zinc-800 palette per decision [11-12])
- Max height with overflow-y-auto, fixed positioning similar to existing Header dropdown

2. src/frontend/src/components/ui/Header.tsx — Update existing component:
- Remove the hardcoded `notifications` array (4 items)
- Import and use `useNotifications` hook
- Replace `unreadCount` with `unreadCount` from hook
- Replace the inline notification dropdown with `<NotificationPanel>` component
- Keep the BellIcon button, badge, and click-to-toggle behavior
- Keep the handleClickOutside logic for closing panel
- The "Ver todas las notificaciones" button should navigate to /pages/notifications using router.push

Follow the existing Header structure — only replace the notification data source and dropdown content.</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>NotificationPanel con AnimatePresence, Header usa useNotifications en vez de datos hardcodeados, tsc sin errores</done>
</task>

<task type="auto">
  <name>Task 3: Create notifications page with full list and actions</name>
  <files>src/frontend/src/app/pages/notifications/page.tsx</files>
  <action>Create "use client" page at src/frontend/src/app/pages/notifications/page.tsx:

- Uses useNotifications hook for data and actions
- Shows paginated list of all notifications using the Table component pattern from Phase 12
- Each row shows: type icon, title, message, date (formatted), read/unread status badge
- Unread rows have subtle background highlight (bg-[#FFF9E6] dark:bg-zinc-800/30)
- "Marcar como leída" action per row (only for unread)
- "Marcar todas como leídas" button in page header
- Pagination controls (previous/next, page number)
- Loading skeleton while fetching (Table skeleton pattern from Phase 13)
- Empty state message when no notifications exist
- Dark mode consistent (zinc palette)
- Page title: "Notificaciones"
- Uses framer-motion AnimatePresence for any modals/confirmations
- Uses toast from sonner for action feedback (per decision [13])
- npx tsc --noEmit and npx next lint must pass</action>
  <verify>
    <automated>npx tsc --noEmit && npx next lint</automated>
  </verify>
  <done>Página /pages/notifications con lista paginada, acciones de marcar como leída, dark mode consistente, tsc y lint sin errores</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes in src/frontend/
- `npx next lint` passes in src/frontend/
- Header bell icon shows real unread count badge
- Clicking bell opens panel with real notifications from API
- Clicking a notification marks it as read (blue dot disappears)
- "Marcar todas como leídas" works from both panel and page
- /pages/notifications shows paginated list
- Polling refreshes unread count every 30 seconds
- No raw `fetch` calls — all API calls through http.ts
</verification>

<success_criteria>
- Notificaciones hardcodeadas del Header reemplazadas por datos reales de API
- Badge del Header muestra conteo real de no leídas
- Panel de notificaciones funcional con animación AnimatePresence
- Página /pages/notifications con lista completa, paginación y acciones
- Polling automático cada 30 segundos para actualizar conteo
- Todo el código TypeScript compila sin errores
- ESLint sin errores nuevos
- Dark mode consistente con tokens zinc del design system
</success_criteria>

<output>
After completion, create `.planning/phases/14-servicio-de-notificaciones/14-02-SUMMARY.md`
</output>
