---
phase: 15-ui-polish-skeletons-y-error-banners-en-todas-las-vistas
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - src/frontend/src/app/pages/payroll/list/page.tsx
  - src/frontend/src/app/pages/payroll/[id]/page.tsx
  - src/frontend/src/app/pages/payroll/[id]/employees/page.tsx
  - src/frontend/src/app/pages/payroll/calculate/page.tsx
  - src/frontend/src/app/pages/attendance/page.tsx
  - src/frontend/src/app/pages/clocklogs/list/page.tsx
  - src/frontend/src/app/pages/employee/list/page.tsx
  - src/frontend/src/app/pages/employee-deductions/list/page.tsx
  - src/frontend/src/app/pages/employee/edit/[id]/page.tsx
  - src/frontend/src/app/pages/audit-logs/page.tsx
  - src/frontend/src/app/pages/notifications/page.tsx
  - src/frontend/src/app/pages/reports/page.tsx
autonomous: true
requirements: [UI-POLISH-01, UI-POLISH-02, UI-POLISH-03]
user_setup: []

must_haves:
  truths:
    - "Usuario ve skeleton loading en vistas complejas (planilla, asistencia, auditoría) mientras cargan datos"
    - "Usuario ve error banner con retry en vistas que usan fetch directo (payroll detail, employee edit)"
    - "Vistas de formulario (employee edit) muestran skeleton del formulario, no spinner genérico"
  artifacts:
    - path: "src/frontend/src/app/pages/payroll/list/page.tsx"
      provides: "Skeleton grid cards for payroll list"
      contains: "animate-pulse"
    - path: "src/frontend/src/app/pages/payroll/[id]/page.tsx"
      provides: "Skeleton detail page + error banner"
      contains: "animate-pulse"
    - path: "src/frontend/src/app/pages/attendance/page.tsx"
      provides: "Skeleton table for attendance"
      contains: "animate-pulse"
    - path: "src/frontend/src/app/pages/audit-logs/page.tsx"
      provides: "Skeleton list for audit logs"
      contains: "animate-pulse"
  key_links:
    - from: "payroll/[id]/page.tsx"
      to: "PayrollService.getPayrollById"
      via: "async loadPayrollDetails with try/catch"
      pattern: "PayrollService\\.getPayrollById"
    - from: "employee/edit/[id]/page.tsx"
      to: "useEmployeeEdit hook"
      via: "isLoading and error from hook"
      pattern: "useEmployeeEdit"
---

<objective>
Add skeleton loading states and error banners to 12 complex pages including payroll, attendance, audit logs, and form pages.

Purpose: Complete UI polish coverage across all remaining data views — complex pages with custom layouts, detail views, and form pages that don't use the Table component.
Output: 12 pages with consistent loading/error states matching their specific layouts.
</objective>

<execution_context>
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/frontend/src/components/ui/Table.tsx
@.planning/phases/13-integracion-frontend-backend/13-03-SUMMARY.md
@.planning/phases/15-ui-polish-skeletons-y-error-banners-en-todas-las-vistas/15-01-PLAN.md

<interfaces>
From src/frontend/src/hooks/usePayroll.ts:
```typescript
// Returns: { getAllPayrolls, isLoading, error }
```

From src/frontend/src/services/payrollService.ts:
```typescript
export interface Payroll {
  id: number;
  payroll_type_id: number;
  period_start: string;
  period_end: string;
  total_employees: number;
  total_gross: number;
  total_deductions: number;
  total_net: number;
  status: string;
  created_at: string;
}
```

From src/frontend/src/hooks/useEmployeeEdit.ts:
```typescript
// Returns: { employee, isLoading, error, update }
```

From src/frontend/src/hooks/useAuditLogs.ts:
```typescript
// Returns: { data: logs, total, isLoading, error, fetchAuditLogs }
```

From src/frontend/src/hooks/useNotifications.ts:
```typescript
// Returns: { notifications, unreadCount, isLoading, markAsRead, markAllAsRead, deleteNotification }
```

Pattern for error banner (from Phase 13 Table.tsx):
```tsx
{error && (
  <div className="mb-4 overflow-auto rounded-lg border border-red-200 dark:border-red-800">
    <div className="bg-red-50 dark:bg-red-950/50 p-6 text-center">
      <ExclamationTriangleIcon className="w-10 h-10 mb-3 text-red-500 dark:text-red-400" />
      <p className="text-sm font-medium text-red-800 dark:text-red-200 mb-1">Error al cargar datos</p>
      <p className="text-xs text-red-600 dark:text-red-400 mb-4">{error}</p>
      <button onClick={retryFn} className="flex items-center gap-2 px-4 py-2 bg-red-600 hover:bg-red-700 text-white rounded-lg text-sm font-medium transition-colors">
        <ArrowPathIcon className="w-4 h-4" />
        Reintentar
      </button>
    </div>
  </div>
)}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add skeleton loading to payroll and attendance pages</name>
  <files>
    src/frontend/src/app/pages/payroll/list/page.tsx
    src/frontend/src/app/pages/payroll/[id]/page.tsx
    src/frontend/src/app/pages/payroll/[id]/employees/page.tsx
    src/frontend/src/app/pages/payroll/calculate/page.tsx
    src/frontend/src/app/pages/attendance/page.tsx
    src/frontend/src/app/pages/clocklogs/list/page.tsx
  </files>
  <action>
    Replace spinner-only loading with structured skeleton loading on 6 pages:

    1. **payroll/list/page.tsx** (lines ~180-185): Replace the spinner div with skeleton cards matching the payroll grid layout. Each skeleton card: rounded-xl border, bg-zinc-200/700 animate-pulse blocks for stats area (3 cards) and payroll items. Show 3 stat skeletons + 4 payroll card skeletons. Each payroll card skeleton: icon area, name, period dates, amount, status badge, action buttons.

    2. **payroll/[id]/page.tsx** (lines ~219): Replace the full-page spinner return with a skeleton detail page. Skeleton header bar (back button, title, actions), skeleton stats row (4 stat cards with animate-pulse), skeleton employee table rows (5 rows). Keep the current `if (isLoading) { return ... }` pattern but with skeleton content instead of spinner.

    3. **payroll/[id]/employees/page.tsx** (lines ~124): Replace spinner with skeleton table rows matching the payroll employee table columns. 5 skeleton rows with animate-pulse divs for each column.

    4. **payroll/calculate/page.tsx**: Currently uses isLoading only for button disable state. Add a skeleton loading section for when `data` is being fetched (the nominee calculation results area). Show skeleton result cards while loadingTypes is true (payroll type dropdown area).

    5. **attendance/page.tsx** (large page): Replace spinner (line ~827) with skeleton table rows matching the attendance table structure. The attendance table has many columns — skeleton rows should have animate-pulse divs for each visible column area. Show 8 skeleton rows.

    6. **clocklogs/list/page.tsx** (line ~384): Currently shows empty state when no data but no loading skeleton. Add skeleton table rows for the clock logs table. Show 6 skeleton rows matching the clock log columns (employee, date, entry, exit, etc.).

    For ALL pages:
    - Use `animate-pulse` + `bg-zinc-200 dark:bg-zinc-700` for skeleton blocks
    - Match the actual content structure (cards for card layouts, table rows for tables)
    - Dark mode variants on ALL skeleton elements
    - Skeletons only during initial fetch, not during mutations

    Addresses review concern: Skeleton loading on all views (UI-POLISH-01)
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    Visual check: Each page shows skeleton placeholders matching content layout during initial load
  </verify>
  <done>All 6 pages show structured skeleton loading instead of generic spinners, dark mode consistent</done>
</task>

<task type="auto">
  <name>Task 2: Add error banners and fix loading on remaining 6 pages</name>
  <files>
    src/frontend/src/app/pages/employee/list/page.tsx
    src/frontend/src/app/pages/employee-deductions/list/page.tsx
    src/frontend/src/app/pages/employee/edit/[id]/page.tsx
    src/frontend/src/app/pages/audit-logs/page.tsx
    src/frontend/src/app/pages/notifications/page.tsx
    src/frontend/src/app/pages/reports/page.tsx
  </files>
  <action>
    Add error banners and improve loading states on 6 remaining pages:

    1. **employee/list/page.tsx**: The employee list page uses a custom card/table layout. Destructure `error` from the employee hook (check useEmployees or equivalent). Add error banner above the employee list. If the page already has skeleton loading via a child component, verify it uses animate-pulse pattern. Add skeleton rows if missing.

    2. **employee-deductions/list/page.tsx**: Uses two hooks (useDeductions + useEmployeeDeductions). Add error banners for both data sources. Show combined error if either fails, or individual errors near their respective sections. Add skeleton loading for the deduction assignment table.

    3. **employee/edit/[id]/page.tsx** (lines 84-112): Currently has spinner (line 84-93) and error page (line 95-112) as full-page returns. 
       - REPLACE the spinner return with a skeleton form: skeleton input fields (animate-pulse rounded-lg divs matching input heights), skeleton select dropdown, skeleton buttons. Show 8-10 skeleton form fields matching the actual form structure.
       - KEEP the error return but update styling to match the error banner pattern (red border, ExclamationTriangleIcon, retry button that navigates back to employee list).
       - The error state should also include a "Reintentar" button that calls the load function again (not just navigate away).

    4. **audit-logs/page.tsx** (lines ~186-190): Replace spinner with skeleton log entries. Each skeleton log entry: animate-pulse divs for action badge, entity badge, username, email, timestamp, details. Show 6 skeleton entries. Add error banner using the `error` from useAuditLogs hook with `fetchAuditLogs` as retry.

    5. **notifications/page.tsx**: Uses `isLoading` from useNotifications hook. Replace inline loading (line ~107) with skeleton notification cards. Each skeleton card: rounded-lg border, animate-pulse blocks for icon, title, message, timestamp, unread indicator. Show 5 skeleton cards. The hook already has `isLoading` — add error banner if the hook exposes error state, or wrap the fetch in error handling.

    6. **reports/page.tsx**: This is likely a static page with report generation buttons. Check if it fetches any data. If it does, add error handling. If it's purely static (no data fetch), skip loading/error states — no action needed for this file.

    Error banner pattern for all pages:
    ```tsx
    {error && (
      <div className="mb-4 overflow-auto rounded-lg border border-red-200 dark:border-red-800">
        <div className="bg-red-50 dark:bg-red-950/50 p-6 text-center">
          <ExclamationTriangleIcon className="w-10 h-10 mb-3 text-red-500 dark:text-red-400" />
          <p className="text-sm font-medium text-red-800 dark:text-red-200 mb-1">Error al cargar datos</p>
          <p className="text-xs text-red-600 dark:text-red-400 mb-4">{error}</p>
          <button onClick={retryFn} className="flex items-center gap-2 px-4 py-2 bg-red-600 hover:bg-red-700 text-white rounded-lg text-sm font-medium transition-colors">
            <ArrowPathIcon className="w-4 h-4" />
            Reintentar
          </button>
        </div>
      </div>
    )}
    ```

    Addresses review concern: Error feedback on all views (UI-POLISH-02)
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    Visual check: All pages show error banners with retry on fetch failure, skeleton loading on initial load
  </verify>
  <done>All remaining pages have skeleton loading and error banners, employee edit page has skeleton form instead of spinner</done>
</task>

</tasks>

<verification>
- npx tsc --noEmit passes in frontend
- npx next lint passes
- All 12 pages show skeleton loading matching their content layout
- All pages with data fetching show error banners with retry on failure
- Dark mode consistent throughout all new UI elements
- No generic spinner-only loading states remain for initial data fetch
</verification>

<success_criteria>
- 12 complex pages have skeleton loading matching their specific layouts
- All data-fetching pages have error banners with retry functionality
- Employee edit page shows skeleton form instead of full-page spinner
- Payroll detail page shows skeleton detail layout instead of spinner
- TypeScript compiles cleanly (only pre-existing errors allowed)
- Dark mode consistent throughout all new UI elements
</success_criteria>

<output>
After completion, create `.planning/phases/15-ui-polish-skeletons-y-error-banners-en-todas-las-vistas/15-02-SUMMARY.md`
</output>
