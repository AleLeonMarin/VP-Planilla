---
phase: 15-ui-polish-skeletons-y-error-banners-en-todas-las-vistas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/frontend/src/app/pages/deductions/list/page.tsx
  - src/frontend/src/app/pages/vacations/list/page.tsx
  - src/frontend/src/app/pages/branches/list/page.tsx
  - src/frontend/src/app/pages/payroll-types/list/page.tsx
  - src/frontend/src/app/pages/employee/events/page.tsx
  - src/frontend/src/app/pages/users/page.tsx
autonomous: true
requirements: [UI-POLISH-01, UI-POLISH-02, UI-POLISH-03]
user_setup: []

must_haves:
  truths:
    - "Usuario ve skeleton loading (no spinner genérico) mientras cargan datos en las 6 vistas"
    - "Usuario ve banner de error rojo con botón 'Reintentar' si la petición falla"
    - "Ninguna vista queda en blanco indefinidamente si el backend no responde"
  artifacts:
    - path: "src/frontend/src/app/pages/deductions/list/page.tsx"
      provides: "Skeleton loading + error banner"
      contains: "animate-pulse"
    - path: "src/frontend/src/app/pages/vacations/list/page.tsx"
      provides: "Skeleton loading + error banner"
      contains: "animate-pulse"
    - path: "src/frontend/src/app/pages/branches/list/page.tsx"
      provides: "Skeleton loading + error banner"
      contains: "animate-pulse"
    - path: "src/frontend/src/app/pages/payroll-types/list/page.tsx"
      provides: "Skeleton loading + error banner"
      contains: "animate-pulse"
    - path: "src/frontend/src/app/pages/employee/events/page.tsx"
      provides: "Skeleton loading + error banner"
      contains: "animate-pulse"
    - path: "src/frontend/src/app/pages/users/page.tsx"
      provides: "Skeleton loading + error banner"
      contains: "animate-pulse"
  key_links:
    - from: "each page.tsx"
      to: "respective hook (useDeductions, useVacations, etc.)"
      via: "isLoading and error destructured from hook"
      pattern: "const.*{.*isLoading.*error.*}.*=.*use"
---

<objective>
Add skeleton loading states and error banners to 6 simple list pages that currently only show spinners.

Purpose: Eliminate inconsistent loading UX — replace generic spinners with structured skeleton loading and add error banners with retry functionality.
Output: 6 pages with consistent loading/error states matching Phase 13 Table component pattern.
</objective>

<execution_context>
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Kendall Fonseca/Desktop/U/Ingeniria en sitemas/VP-Planilla/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/frontend/src/components/ui/Table.tsx
@.planning/phases/13-integracion-frontend-backend/13-03-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/frontend/src/hooks/useDeductions.ts:
```typescript
// Returns: { data, isLoading, error, refetch, create, update, remove }
```

From src/frontend/src/hooks/useVacations.ts:
```typescript
// Returns: { data, isLoading, error, refetch, remove }
```

From src/frontend/src/hooks/useBranches.ts:
```typescript
// Returns: { data: branches, isLoading, error, create, update, remove, refetch }
```

From src/frontend/src/hooks/usePayrollTypes.ts:
```typescript
// Returns: { data, isLoading, error, refetch, create, update, remove }
```

From src/frontend/src/hooks/useLaborEvents.ts:
```typescript
// Returns: { events, isLoading, createEvent, updateEvent, refreshEvents, deleteAssignment }
```

From src/frontend/src/hooks/useUsers.ts:
```typescript
// Returns: { data, isLoading, error, refetch, create, update, remove }
```

Pattern to follow (from Table.tsx error banner):
```tsx
// Error state: red banner with retry button
<div className="overflow-auto rounded-lg border border-red-200 dark:border-red-800">
  <div className="bg-red-50 dark:bg-red-950/50 p-6 text-center">
    <svg className="w-10 h-10 mb-3 text-red-500 dark:text-red-400" ...>...</svg>
    <p className="text-sm font-medium text-red-800 dark:text-red-200 mb-1">Error al cargar datos</p>
    <p className="text-xs text-red-600 dark:text-red-400 mb-4">{error}</p>
    {onRetry && <button onClick={onRetry}>Reintentar</button>}
  </div>
</div>

// Loading state: skeleton with animate-pulse
<div className="animate-pulse">
  <div className="h-4 bg-zinc-200 dark:bg-zinc-700 rounded w-3/4"></div>
</div>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace spinners with skeleton loading in 6 list pages</name>
  <files>
    src/frontend/src/app/pages/deductions/list/page.tsx
    src/frontend/src/app/pages/vacations/list/page.tsx
    src/frontend/src/app/pages/branches/list/page.tsx
    src/frontend/src/app/pages/payroll-types/list/page.tsx
    src/frontend/src/app/pages/employee/events/page.tsx
    src/frontend/src/app/pages/users/page.tsx
  </files>
  <action>
    For each of the 6 list pages, replace the current spinner-only loading state with structured skeleton loading that matches the content layout:

    1. **deductions/list/page.tsx** — Currently shows spinner in centered box. Replace with skeleton cards matching the card grid layout (3 cols). Each skeleton card: rounded-lg border, bg-zinc-200/700 animate-pulse divs for header icon area, title area, and action buttons. Show 6 skeleton cards.

    2. **vacations/list/page.tsx** — Currently shows spinner in centered box. Replace with skeleton table rows matching the vacation table columns. Use animate-pulse divs in a table structure with 5 skeleton rows.

    3. **branches/list/page.tsx** — Currently shows spinner in centered box. Replace with skeleton cards matching the branch card layout. Each skeleton: rounded-lg border, animate-pulse divs for icon, name, address, employees count, action buttons. Show 4 skeleton cards.

    4. **payroll-types/list/page.tsx** — Currently shows spinner in centered box. Replace with skeleton table rows matching the payroll types table. Use animate-pulse divs in table structure with 5 skeleton rows.

    5. **employee/events/page.tsx** — Currently passes isLoading to Table component (already wired). Verify the Table component renders skeletons correctly. If events page uses custom rendering instead of Table, add skeleton rows.

    6. **users/page.tsx** — Currently shows spinner. Replace with skeleton table rows matching the users table structure. Use animate-pulse divs with 5 skeleton rows.

    For ALL pages:
    - Skeletons MUST use `animate-pulse` from Tailwind
    - Skeletons MUST use `bg-zinc-200 dark:bg-zinc-700` for placeholder blocks
    - Skeletons MUST match the column/card structure of the actual content
    - Skeletons MUST only show during initial load (isLoading === true), NOT during mutations
    - Keep dark mode variants consistent: `dark:bg-zinc-700` for skeleton blocks, `dark:bg-zinc-800` for card backgrounds

    Addresses review concern: Consistent loading states across all views (UI-POLISH-01)
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    Visual check: Each page shows skeleton placeholders (not spinner) during initial load
  </verify>
  <done>All 6 pages show skeleton loading matching their content layout, no generic spinners remain for initial load state</done>
</task>

<task type="auto">
  <name>Task 2: Add error banners with retry to all 6 list pages</name>
  <files>
    src/frontend/src/app/pages/deductions/list/page.tsx
    src/frontend/src/app/pages/vacations/list/page.tsx
    src/frontend/src/app/pages/branches/list/page.tsx
    src/frontend/src/app/pages/payroll-types/list/page.tsx
    src/frontend/src/app/pages/employee/events/page.tsx
    src/frontend/src/app/pages/users/page.tsx
  </files>
  <action>
    For each of the 6 list pages, add an error banner that displays when the hook's `error` state is populated:

    Error banner pattern (copy from Table.tsx error state):
    ```tsx
    {error && (
      <div className="mb-4 overflow-auto rounded-lg border border-red-200 dark:border-red-800">
        <div className="bg-red-50 dark:bg-red-950/50 p-6 text-center">
          <div className="flex flex-col items-center">
            <ExclamationTriangleIcon className="w-10 h-10 mb-3 text-red-500 dark:text-red-400" />
            <p className="text-sm font-medium text-red-800 dark:text-red-200 mb-1">Error al cargar datos</p>
            <p className="text-xs text-red-600 dark:text-red-400 mb-4">{error}</p>
            <button
              onClick={refetch}
              className="flex items-center gap-2 px-4 py-2 bg-red-600 hover:bg-red-700 text-white rounded-lg text-sm font-medium transition-colors"
            >
              <ArrowPathIcon className="w-4 h-4" />
              Reintentar
            </button>
          </div>
        </div>
      </div>
    )}
    ```

    For each page:
    1. Import `ExclamationTriangleIcon` and `ArrowPathIcon` from `@heroicons/react/24/outline` if not already imported
    2. Destructure `error` and `refetch` (or equivalent refresh function) from the hook
    3. Place error banner ABOVE the data list/table, with `mb-4` spacing
    4. Ensure error banner does NOT show when isLoading is true (error state takes priority over loading)
    5. For employee/events page: use `refreshEvents` as the retry function
    6. For pages that already have an error div (deductions has one at line 96-101): REPLACE the existing simple error div with the full banner pattern above

    Conditional rendering order MUST be: error first, then loading/skeletons, then data/empty state.
    This ensures error state takes priority and user sees the problem immediately.

    Addresses review concern: Error feedback on all views (UI-POLISH-02)
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    Visual check: Each page shows red error banner with "Reintentar" button when error state is set
  </verify>
  <done>All 6 pages display structured error banners with retry button, error state takes priority over loading state</done>
</task>

</tasks>

<verification>
- npx tsc --noEmit passes in frontend
- npx next lint passes
- All 6 pages show skeleton loading (not spinner) during initial data fetch
- All 6 pages show error banner with retry button on fetch failure
- Dark mode variants present on all skeleton and error elements
</verification>

<success_criteria>
- 6 list pages have consistent skeleton loading matching their content layout
- 6 list pages have error banners with retry functionality
- No generic spinner-only loading states remain for initial data fetch
- TypeScript compiles cleanly (only pre-existing errors allowed)
- Dark mode consistent throughout all new UI elements
</success_criteria>

<output>
After completion, create `.planning/phases/15-ui-polish-skeletons-y-error-banners-en-todas-las-vistas/15-01-SUMMARY.md`
</output>
